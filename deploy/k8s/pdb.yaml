apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: hydrahouse-api
  namespace: hydrahouse
  labels:
    app: hydrahouse
    component: api
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: hydrahouse
      component: api
